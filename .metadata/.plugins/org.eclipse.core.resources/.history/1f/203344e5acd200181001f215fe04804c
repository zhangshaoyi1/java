package java_io;

import java.io.File;

class copyFile {
	 /**
	  * 拷贝文件
	  * @param isPath 数据来源
	  * @param doesPath 数据去向
	  */
	public static void copy(String isPath,String doesPath) {
		File is = new File(isPath);
		File does = new File(doesPath);
		
	}
}

